import { ComingSoon } from "@/components/shared/ComingSoon";

export default function BookTripPage() {
  return (
    <ComingSoon
      title="Book Trip"
      phase="Phase 4"
      description="Booking form with radio card type selector, locked source field, and identity context panel."
    />
  );
}
