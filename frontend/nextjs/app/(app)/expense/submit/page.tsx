import { ComingSoon } from "@/components/shared/ComingSoon";

export default function SubmitExpensePage() {
  return (
    <ComingSoon
      title="Submit Expense"
      phase="Phase 5"
      description="Multi-item expense form with dynamic line items, running total, and identity context panel."
    />
  );
}
