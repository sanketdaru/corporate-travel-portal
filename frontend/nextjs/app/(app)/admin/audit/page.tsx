import { ComingSoon } from "@/components/shared/ComingSoon";

export default function AuditPage() {
  return (
    <ComingSoon
      title="Audit Log"
      phase="Phase 7"
      description="Paginated audit events with DENY row highlighting and actor/subject filtering."
    />
  );
}
